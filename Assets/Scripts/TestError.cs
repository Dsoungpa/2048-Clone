using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestError : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Debug.LogError("My First Error!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
